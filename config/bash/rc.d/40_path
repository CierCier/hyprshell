case $- in
  *i*) ;;
  *) return ;;
esac

set +o errexit

path_append() {
  [ -n "$1" ] || return 1
  case ":$PATH:" in
    *":$1:"*) return 0 ;;
    *) PATH="$PATH:$1" ;;
  esac
}



path_append ~/.local/bin
path_append ~/.cargo/bin
path_append ~/.bun/bin

