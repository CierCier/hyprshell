
general {
    col.active_border = rgba(EF66B6AA)
    col.inactive_border = rgba(B3B2BF30)
}

misc {
    background_color = rgba(B3B2BFFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(58BC5CAA)
        col.text = rgba(57665FFF)


        # example buttons (R -> L)
        #hyprbars-button = color, size, on-click
        hyprbars-button = rgba(EF66B6FF), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgba(EF66B6FF), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgba(EF66B6FF), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(EF66B6AA) rgba(0B1438AA),pinned:1
